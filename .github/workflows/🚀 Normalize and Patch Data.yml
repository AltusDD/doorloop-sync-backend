name: 🚀 Normalize and Patch Data

on:
  workflow_dispatch:

jobs:
  normalize_and_patch:
    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Checkout Repo
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔍 Debug Working Directory and File Contents
        run: |
          echo "--- Working Directory ---"
          pwd
          echo "--- Root Contents ---"
          ls -F
          echo "--- scripts/ Directory ---"
          ls -F scripts/
          echo "--- Stat File ---"
          stat "${{ github.workspace }}/scripts/normalize_and_patch_all.py" || echo "File not found!"

      # 🚀 Run Normalization Pipeline task removed for local/manual testing only
