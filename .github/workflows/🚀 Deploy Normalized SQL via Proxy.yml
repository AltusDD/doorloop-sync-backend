name: 🚀 Deploy Normalized SQL via Proxy

on:
  workflow_dispatch:

jobs:
  deploy-sql:
    runs-on: ubuntu-latest
    env:
      PROJECT_REF: ${{ secrets.PROJECT_REF }}
      SQL_PROXY_URL: ${{ secrets.SQL_PROXY_URL }}
      SQL_PROXY_SECRET: ${{ secrets.SQL_PROXY_SECRET }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install jq
        run: sudo apt-get install jq

      - name: Deploy SQL files via Proxy
        run: |
          echo "📦 Deploying SQL files via proxy to Supabase project: $PROJECT_REF"
          echo "📁 Using SQL directory: ./scripts"
          
          for file in ./scripts/*.sql; do
            filename=$(basename "$file")
            echo "🚀 Applying: $filename"
            SQL_CONTENT=$(cat "$file" | jq -Rs .)

            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "$SQL_PROXY_URL"               -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"               -H "apikey: $SUPABASE_SERVICE_ROLE_KEY"               -H "x-proxy-secret: $SQL_PROXY_SECRET"               -H "Content-Type: application/json"               -d "{"sql": $SQL_CONTENT}"
            )

            HTTP_STATUS=$(echo "$RESPONSE" | tail -n1)
            RESPONSE_BODY=$(echo "$RESPONSE" | sed '$ d')

            if [ "$HTTP_STATUS" -eq 200 ]; then
              echo "✅ Success: $filename"
            else
              echo "❌ Failed: $filename (HTTP $HTTP_STATUS)"
              echo "Response: $RESPONSE_BODY"
              exit 1
            fi
          done