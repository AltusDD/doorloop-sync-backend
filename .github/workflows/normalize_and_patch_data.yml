name: Normalize and Patch Data

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  normalize-and-validate:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repo
        uses: actions/checkout@v3

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install Python Dependencies
        run: |
          pip install pg8000

      - name: 🧠 Normalize and Patch Data
        run: |
          echo "🔧 Replace this with your actual normalization logic"
          # python cli/normalize_and_patch_all.py
          # Add any other relevant commands here

      - name: 🛠 Run Supabase Cascade Validation
        run: |
          python scripts/supabase_pg_runner.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
