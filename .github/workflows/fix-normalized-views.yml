name: Fix and Deploy All Views

on:
  workflow_dispatch:

jobs:
  fix-all-views:
    runs-on: ubuntu-latest
    env:
      PROJECT_REF: ${{ secrets.PROJECT_REF }}
      SQL_PROXY_SECRET: ${{ secrets.SQL_PROXY_SECRET }}
    steps:
      - name: 🧾 Checkout repo
        uses: actions/checkout@v3

      - name: 🛠️ Make fix script executable
        run: chmod +x fix_all_normalized_views.sh

      - name: 🚀 Run fix and deploy script
        run: ./fix_all_normalized_views.sh
